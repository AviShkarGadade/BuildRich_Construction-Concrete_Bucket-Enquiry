import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import "./globals.css"

export const metadata: Metadata = {
  title: "Tower Crane Concrete Bucket",
  description:
    "Buy affordable concrete pouring equipment in India. Reduce concrete wastage by 50% with BuildRich tower crane concrete buckets. Professional construction solutions.",
  keywords:
    "concrete pouring bucket, tower crane bucket, construction equipment India, reduce concrete wastage, affordable concrete equipment, BuildRich",
  generator: "BuildRich",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <head>
        <style>{`
html {
  font-family: ${GeistSans.style.fontFamily};
  --font-sans: ${GeistSans.variable};
  --font-mono: ${GeistMono.variable};
}
        `}</style>
      </head>
      <body>{children}</body>
    </html>
  )
}
